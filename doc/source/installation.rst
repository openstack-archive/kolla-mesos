============
Installation
============

At the command line::

    $ pip install kolla-mesos

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kolla-mesos
    $ pip install kolla-mesos
